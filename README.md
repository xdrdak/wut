# wut

**wut** is a dependency free python script that allows you to save custom commands on a per git repository basis.

## Usage

```bash
# Initialize config
wut init

# Add a command interactively
wut add

# List commands for current repo
wut dis

# Run a command (fuzzy search)
wut run
```